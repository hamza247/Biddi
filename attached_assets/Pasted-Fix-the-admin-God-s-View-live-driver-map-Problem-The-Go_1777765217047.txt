Fix the admin “God’s View” live driver map.

Problem:
The God’s View page shows the driver list and counts, but the map does not correctly show available and not available drivers. I already tried fixing it many times, so do not guess. Debug the complete data flow from database/API/socket/frontend/map markers and fix the real root cause.

Important:
- Do NOT rebuild the app.
- Do NOT replace the whole map page unless absolutely necessary.
- Do NOT use fake/static drivers.
- Do NOT hardcode Morocco coordinates except as map fallback center.
- Fix the root cause.
- Keep existing admin design and features.
- After fixing, review the code and test.

Expected behavior:
- Available drivers appear on the map with one marker style/color.
- Not available drivers appear on the map with another marker style/color.
- Drivers on trip/statuses like way to pickup, arrived, way to dropoff appear correctly if supported.
- Driver list on the left and map markers must use the same data source.
- Counts must match the filtered driver list and map markers.
- Clicking a driver in the list should focus/zoom the map to that driver if possible.
- Clicking a marker should show driver name, phone, vehicle, plate, status, and last location time.
- Map should update when driver location/status changes.
- No marker should show at wrong/default location because of missing lat/lng.
- No duplicate markers.

Step 1 — Find the God’s View code

Search for:
- God’s View
- god view
- gods view
- live map
- driver map
- available drivers
- not available drivers
- driver status
- online drivers
- live tracking
- google map
- map markers
- marker
- driver latitude
- driver longitude
- socket

Find:
- God’s View admin page/component
- API route used by this page
- Backend query/controller
- Driver location table/model
- Driver status fields
- Socket.IO events if used
- Marker rendering logic
- Filter/status cards logic

Step 2 — Compare list data vs map marker data

This is the key debug step.

Check if:
- The left driver list receives drivers correctly
- The map receives the same driver array
- Markers are created from the same driver array
- Marker rendering is blocked by a wrong condition
- Lat/lng field names are wrong
- Lat/lng values are strings and not converted to numbers
- Driver status values do not match frontend filters
- Available/not available filter labels do not match backend status values
- Drivers with valid coordinates are accidentally filtered out
- Map loads before driver data is ready
- Google Maps instance is not ready when markers are created
- Markers are created but hidden behind wrong bounds/zoom
- Marker icons are missing/broken
- Marker cleanup removes new markers
- Old markers are not removed before re-render

Add temporary safe debug logs while fixing:
- Number of drivers returned by API
- Number of drivers with valid coordinates
- Number of available drivers
- Number of not available drivers
- Number of markers created
- Example lat/lng/status for first few drivers

Remove or reduce noisy logs after fixing.

Step 3 — Validate API response

Inspect the API response used by God’s View.

The frontend needs clean driver data like:

{
  "id": "123",
  "name": "Driver Name",
  "phone": "+212...",
  "vehicle": "Peugeot 308",
  "plate": "3192-b-59",
  "status": "available",
  "lat": 33.5731,
  "lng": -7.5898,
  "lastLocationAt": "2026-05-02T..."
}

Fix mapping if backend returns different fields, for example:
- latitude / longitude
- lat / lng
- vLatitude / vLongitude
- current_lat / current_lng
- driver_lat / driver_lng
- online_status
- availability_status
- eStatus
- vAvailability
- is_available

Normalize the response before rendering.

Create a helper if needed:

normalizeDriverForMap(driver)

It should return:
- id
- name
- phone
- vehicle
- plate
- status
- lat as number
- lng as number
- isAvailable boolean
- lastLocationAt

Step 4 — Coordinate validation

Create or fix coordinate validation:

isValidCoordinate(lat, lng)

Valid:
- lat is number
- lng is number
- lat between -90 and 90
- lng between -180 and 180
- not null
- not undefined
- not empty string
- not 0,0 unless truly valid for the business

Convert strings to numbers safely:

const latNum = Number(lat)
const lngNum = Number(lng)

Do not render markers for invalid coordinates.
Instead show those drivers in the list with “location unavailable”.

Step 5 — Status normalization

Normalize all driver statuses into stable map statuses:

available
not_available
way_to_pickup
arrived_pickup
way_to_dropoff
offline
unknown

Map backend values carefully.

Examples:
- available, Available, AVAILABLE, active, online, free, 1, true → available
- not_available, busy, offline, inactive, unavailable, 0, false → not_available
- on_trip, trip_started, way_to_dropoff → way_to_dropoff
- accepted, assigned, way_to_pickup → way_to_pickup
- arrived, reached_pickup → arrived_pickup

Use this normalized status for:
- Counts
- Filter cards
- Marker icon/color
- Driver list border/color
- Popup label

Step 6 — Marker rendering fix

Fix marker rendering so that:

- Markers are created only after map is ready
- Markers are created only for drivers with valid coordinates
- Existing markers are cleared before new markers are rendered
- Marker keys use driver id
- Marker icons load correctly
- Marker position uses normalized lat/lng numbers
- Marker status controls marker icon/color
- Map bounds fit visible markers
- If only one marker exists, center on it with a reasonable zoom

If using Google Maps React:
- Ensure Marker components render inside GoogleMap
- Ensure drivers.map() is inside the map component
- Use stable key: driver.id
- Avoid returning null accidentally for valid drivers

If using raw Google Maps API:
- Store markers in ref/state
- Clear old markers with marker.setMap(null)
- Create new google.maps.Marker for each valid driver
- Set marker.setMap(map)
- Add info windows safely

Step 7 — Filter card behavior

The cards should filter both list and map.

Cards:
- Available
- Not Available
- Way to Pickup
- Arrived / Reached Pickup
- Way to Dropoff

When user clicks a card:
- Update active filter
- List updates
- Map markers update
- Counts remain correct
- Selected card has active style

If no filter is selected, show all drivers with valid locations on map and all drivers in list.

Step 8 — Search behavior

Search Driver should filter both:
- Driver list
- Map markers

Search by:
- driver name
- phone
- vehicle
- plate number

Step 9 — Real-time updates

If Socket.IO exists, inspect and fix:
- Driver location update event
- Driver status update event
- Admin joins correct room/channel
- Listener event names match backend emit names
- Driver id matches existing driver id
- Driver state updates correctly
- Markers re-render after socket update
- List counts update after socket update
- Clean up listeners on unmount
- Avoid duplicate socket listeners

Search for event names:
- driverLocationUpdated
- updateDriverLocation
- driverStatusChanged
- driverOnline
- driverOffline
- adminGodViewUpdate
- liveDrivers
- updateLiveDrivers

If socket is broken, make the page still work with polling every 10–15 seconds as fallback.

Step 10 — Backend query checks

Inspect backend query for God’s View.

Fix if:
- It only returns drivers but not coordinates
- It only returns available drivers
- It filters out not available drivers
- It uses wrong table for location
- It does not join driver vehicle data
- It uses old/stale location fields
- It returns null lat/lng
- It does not include last location update time

The query should return all relevant drivers for admin God’s View:
- available
- not available
- on trip statuses
- with latest location when available

Do not show drivers with no location as map markers, but keep them in list with location unavailable.

Step 11 — UI fallback states

Add clear states:

- Loading drivers...
- No drivers found
- No drivers with valid location
- Map loading...
- Location unavailable
- Last updated: time

The map should not appear broken or empty without explanation.

Step 12 — Test checklist

Test exactly:

1. Open God’s View page
2. API returns drivers
3. Left counts show correct numbers
4. Left list shows drivers
5. Available drivers appear on map
6. Not available drivers appear on map
7. Drivers with invalid location do not create bad markers
8. Clicking Available filters list and map
9. Clicking Not Available filters list and map
10. Search filters list and map
11. Clicking driver list item centers map on driver
12. Clicking marker opens popup
13. Socket update moves marker, if socket exists
14. Polling fallback updates markers if socket unavailable
15. No duplicate markers after updates
16. No marker appears at 0,0
17. No console errors
18. No broken image/icon errors
19. Existing admin pages still work

Step 13 — Final code review

After fixing, review all changed files.

Check:
- No broken imports
- No TypeScript/JavaScript errors
- No duplicate socket listeners
- No memory leaks
- No fake data
- No hardcoded driver coordinates
- No exposed secrets
- No repeated API calls loop
- No mismatch between counts, list, and map
- Clean helper functions
- Clean error handling

Final response:
Provide:
- Root cause found
- Files changed
- Backend changes
- Frontend changes
- Status mapping used
- Coordinate fields used
- How markers are rendered now
- How real-time update works
- Tests completed
- Remaining limitations if any