Check, fix, and redesign the Admin “God’s View / Live Map” page.

Problem:
The current God’s View page sometimes shows a blank gray Leaflet map with the message:
“2 drivers found, but none are sharing a live location.”

Reference:
I want it to look more like the second uploaded image:
- Clean live map
- Google-style road layer
- Car icon marker on the map
- Drivers clearly visible
- Professional admin dashboard UI

Important:
- Do NOT use fake driver markers.
- Do NOT hardcode coordinates.
- Do NOT only redesign the UI.
- First fix the real data/map problem, then redesign.
- Keep existing admin permissions and structure.

Main goal:
God’s View should show real online/available drivers on the map with car markers, driver status, live location, and clean controls.

Step 1 — Debug the blank map

Inspect:
- Map component
- Tile layer
- Leaflet/Google map setup
- Map container height
- CSS imports
- API response
- Driver location data
- Marker rendering
- Console errors

Fix issues like:
- map container has no height
- Leaflet CSS missing
- invalid lat/lng
- marker icon missing
- tile layer not loading
- map initialized before container is ready
- wrong coordinate field mapping
- drivers counted but no live coordinates returned

Step 2 — Fix driver live location source

Trace full flow:

Driver app → location update → backend → database → admin API → God’s View map.

Check:
- Driver app sends lat/lng
- Backend saves lat/lng
- Admin API returns lat/lng
- Frontend converts lat/lng to numbers
- Map renders marker only for valid coordinates

Create helper:

normalizeDriverForGodView(driver)

Return:
- id
- name
- phone
- vehicle
- plate
- status
- lat
- lng
- lastLocationAt
- hasLiveLocation
- isStaleLocation

Coordinate validation:
- lat/lng required
- convert to Number
- reject invalid values
- reject 0,0 unless valid
- show drivers with no location in list, but not as markers

Step 3 — Redesign God’s View UI

Create a clean admin layout:

Top stats cards:
- Total Drivers
- Online Drivers
- Available Drivers
- Active Rides

Main layout:
- Large map area
- Right-side driver list panel
- Search driver input
- Status filter dropdown
- Refresh/pulse speed control
- Recenter button

Driver state legend:
- Available
- Not Available
- Way to Pickup
- Arrived at Pickup
- Way to Dropoff
- Stale Location
- Offline

Step 4 — Map style

Use a clean Google-style map layer.

If Google Maps is already used:
- use mapTypeId: "roadmap"

If Leaflet is used:
- use a clean light tile layer
- prefer Carto Light or standard OSM
- keep it stable and fast

Do not mix multiple base layers.

Step 5 — Car icon markers

Add car icon markers like the reference image.

Marker behavior:
- Available driver: blue/green car icon
- Busy/on-trip driver: orange car icon
- Not available/offline: gray car icon
- Stale location: red warning marker

Marker popup should show:
- Driver name
- Phone
- Vehicle type
- Plate number
- Status
- Last location update
- Current trip id if active

Clicking driver in list:
- centers map on driver
- opens marker popup if possible

Clicking marker:
- highlights driver in list

Step 6 — Filters and search

Filters must affect both:
- driver list
- map markers

Search by:
- name
- phone
- plate
- vehicle

Default filter:
- All drivers with valid live location
Do not default to “Way to dropoff” because it hides drivers.

Step 7 — Realtime updates

Use existing Socket.IO if available.

Events:
- driver:location:update
- driver:status:update
- ride:status:update

If socket is missing or unreliable:
- add polling every 10 seconds

Rules:
- update marker position without full page reload
- no duplicate markers
- clean socket listeners on unmount
- show “last updated X seconds ago”

Step 8 — Empty and error states

Show clear messages:

If map loads but no live locations:
“Drivers found, but no valid live location yet.”

If API fails:
“Unable to load live map data.”

If driver has stale location:
Show in list with stale badge.

Do not show blank map without explanation.

Step 9 — Backend/API requirements

Create or fix endpoint:

GET /admin/gods-view/drivers

Response:

{
  "success": true,
  "stats": {
    "totalDrivers": 2,
    "onlineDrivers": 1,
    "availableDrivers": 1,
    "activeRides": 0
  },
  "drivers": [
    {
      "id": 1,
      "name": "Driver Name",
      "phone": "+212...",
      "vehicle": "Peugeot 308",
      "plate": "3192-b-59",
      "status": "available",
      "lat": 35.7595,
      "lng": -5.8340,
      "lastLocationAt": "2026-05-06T..."
    }
  ]
}

Step 10 — Testing

Test:
1. God’s View opens without blank map.
2. Base map loads correctly.
3. Real driver markers appear.
4. Car icon marker appears like the reference.
5. Drivers with no location stay in list but not map.
6. Counts match data.
7. Search filters list and map.
8. Status filter filters list and map.
9. Clicking list item centers map.
10. Clicking marker opens popup.
11. Driver location update moves marker.
12. No duplicate markers.
13. No invalid LatLng errors.
14. No console errors.
15. Existing admin pages still work.

Final code review:
- No fake coordinates
- No hardcoded drivers
- No broken imports
- No duplicate socket listeners
- No memory leaks
- Map container has stable height
- Marker icons load correctly
- API response is normalized
- UI is responsive

Final report:
Provide:
- Root cause of blank map
- Root cause of missing driver markers
- Files changed
- API changes
- Map layer used
- Marker icon implementation
- Status mapping
- Tests completed
- Remaining limitations