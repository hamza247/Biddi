You are working on my existing rideshare / delivery admin panel project.

I uploaded a SQL backup file:

backup_2025_07_24_00_51.sql

Your task is to analyze the full SQL schema first, understand all existing tables, relationships, modules, and admin features, then improve the existing app UI/UX and admin workflows from A to Z.

Important:
Do NOT rebuild the app from scratch.
Do NOT remove existing features.
Do NOT randomly rename existing tables/columns.
Work with the current project structure, database style, backend routes, frontend/admin UI, permissions, and existing business logic.

Main Goal:
Analyze the existing SQL schema and improve the admin panel UI/UX, navigation, forms, tables, settings pages, fare configuration, vehicle configuration, geofencing/zone configuration, and overall admin usability.

Step 1 — SQL Schema Analysis

First, carefully inspect the SQL file and produce an internal understanding of:

- All tables
- Primary keys
- Foreign-key-like relationships, even if no formal FK exists
- Admin tables
- User/passenger tables
- Driver tables
- Vehicle tables
- Trip/ride tables
- Fare/pricing tables
- Currency tables
- Country/state/city/location tables
- Zone/location fare tables
- Payment tables
- Wallet tables
- Coupon/reward tables
- Notification/SMS/email template tables
- Language/localization tables
- Permission/admin group tables
- Delivery/rental/parking/bidding modules if they exist

Pay special attention to tables like:

- administrators
- admin_groups
- admin_permissions
- admin_group_permission
- configurations
- configurations_payment
- currency
- master_currency
- country
- state
- city
- location_master
- location_wise_fare
- restricted_negative_area
- vehicle_type
- vehicle_category
- master_vehicle_category
- driver_vehicle
- register_driver
- register_user
- trips
- cab_booking
- driver_request
- payments
- payment_transactions
- user_wallet
- coupon
- weather_surcharge
- airportsurcharge_fare

Create a schema map before coding.

Step 2 — Understand Existing App

Review the existing codebase and identify:

- Admin panel framework/language
- Backend routes/controllers
- Database connection method
- Existing admin layout/sidebar/menu
- Existing permission system
- Existing form style
- Existing table/list style
- Existing validation style
- Existing notification/toast style
- Existing map usage, if any
- Existing fare/vehicle/location configuration pages

Do not assume. Inspect the files.

Step 3 — UI/UX Audit

Audit the current admin panel and identify UI/UX problems:

- Confusing navigation
- Too many scattered settings
- Repeated forms
- Poor table readability
- Missing search/filter/sort
- Missing status badges
- Missing confirmation modals
- Poor mobile/tablet responsiveness
- Missing loading states
- Missing empty states
- Missing validation messages
- Missing success/error feedback
- Poor grouping of related settings
- Complicated fare setup flow
- Confusing vehicle type setup
- Confusing location/zone setup

Then improve the UI/UX directly in the code.

Step 4 — Admin Navigation Improvements

Improve the admin sidebar/menu by grouping related modules clearly.

Suggested groups:

Dashboard
- Overview
- Reports

Users
- Riders
- Drivers
- Companies
- Documents

Rides & Trips
- Trips
- Bookings
- Requests
- Cancellations
- Ratings

Fleet & Vehicle Setup
- Vehicle Types
- Vehicle Categories
- Driver Vehicles
- Vehicle Fare Settings

Pricing & Zones
- Geofencing Zones
- Location Wise Fare
- Airport Surcharge
- Weather Surcharge
- Currency Settings
- Coupons

Payments & Wallet
- Payments
- Payment Transactions
- Wallet
- Withdraw Requests

Content & Communication
- Push Notifications
- SMS Templates
- Email Templates
- Pages
- FAQs
- Banners

System Settings
- General Configurations
- Payment Configurations
- Languages
- Admin Users
- Admin Groups
- Permissions

Only add or update menus based on existing permission logic.

Step 5 — Add/Improve Geofencing Zone UI

Based on the existing schema, check if tables already exist for zones/location fares/restricted areas.

Existing related tables may include:
- location_master
- location_wise_fare
- restricted_negative_area
- city
- country
- currency
- master_currency
- vehicle_type
- vehicle_category

If existing tables can support the feature, reuse them.
If not, create safe new tables using migrations.

Add or improve an admin page called:

Geofencing Zones

Admin should be able to:

- View all zones
- Search zones
- Filter by status
- Add new zone
- Edit zone
- Delete zone
- Activate/deactivate zone
- Draw zone on map
- Choose geometry type
- Set distance unit
- Set currency
- Configure vehicle fares per zone

Map drawing should support:

- Polygon
- Circle
- Rectangle

Use Leaflet + OpenStreetMap unless the project already uses another map library.

Store geometry as GeoJSON where possible.

Each zone should support:

- Zone name
- Description
- Geometry type
- Geometry data
- Distance unit: kilometers / miles
- Currency: MAD / USD / EUR / CAD / other currencies from database
- Status active/inactive

Step 6 — Vehicle Type & Fare Configuration

Improve vehicle type and fare setup.

Admin should be able to configure fares per zone and per vehicle type.

Use existing vehicle tables if available:
- vehicle_type
- vehicle_category
- master_vehicle_category
- vehicle_type_uberx_3
- vehicle_size_price_info
- location_wise_fare

Each fare configuration should include:

- Vehicle type
- Base fare
- Price per distance unit
- Price per minute
- Minimum fare
- Cancellation fee
- Waiting fee per minute
- Driver commission percentage
- Rider service fee
- Active/inactive status

The UI should make this easy:
- Use cards or clean table rows
- Add inline validation
- Add clear currency/unit labels
- Add duplicate prevention
- Add save/cancel buttons
- Add confirmation before delete

Step 7 — Settings Dropdowns

Add clean dropdown lists for:

Distance unit:
- kilometers
- miles

Currency:
Use currencies from the existing database if available.
Fallback options:
- MAD
- USD
- EUR
- CAD

Vehicle types:
Use existing active vehicle types from the database.
Fallback default:
- Economy
- Comfort
- XL
- Premium
- Delivery

Step 8 — Backend/API Improvements

Add or improve backend endpoints for:

Zones:
- Get all zones
- Get one zone
- Create zone
- Update zone
- Delete zone
- Activate/deactivate zone

Zone fares:
- Get fares for zone
- Add fare for vehicle type
- Update fare
- Delete fare

Helpers:
- findZoneByCoordinates(lat, lng)
- calculateFareForZone(zoneId, vehicleTypeId, distance, durationMinutes)

Validation:
- Zone name required
- Geometry required
- Geometry type required
- Currency required
- Distance unit required
- Fare numbers must be valid and positive
- Commission must be between 0 and 100
- Vehicle type must exist
- Prevent duplicate fare config for same zone + vehicle type

Step 9 — UI/UX Quality Requirements

Improve all new and touched pages with:

- Clean page titles
- Breadcrumbs where useful
- Search bar
- Filters
- Pagination
- Sortable columns where possible
- Status badges
- Loading states
- Empty states
- Error states
- Confirmation modals
- Success/error toast messages
- Better form spacing
- Mobile-responsive layout
- Clear labels
- Helper text for complex fields
- Consistent buttons
- Consistent colors
- Consistent icons if project uses icons

Step 10 — Code Quality

After finishing implementation, review the code.

Check:

- No broken imports
- No duplicate functions
- No unused code
- No syntax errors
- No broken routes
- No broken admin login
- No broken sidebar
- No broken permissions
- No broken existing fare logic
- No broken existing vehicle logic
- No broken existing currency logic
- No SQL errors
- No hardcoded sensitive values
- Proper error handling
- Proper validation
- Clean reusable components
- Clean reusable services/helpers

Step 11 — Testing

Test manually:

1. Admin login still works
2. Dashboard loads
3. Sidebar loads
4. Existing pages still load
5. Geofencing Zones page loads
6. Admin can draw polygon zone
7. Admin can draw circle zone
8. Admin can draw rectangle zone
9. Admin can save zone
10. Admin can edit zone
11. Admin can delete zone
12. Admin can activate/deactivate zone
13. Currency dropdown works
14. Distance unit dropdown works
15. Vehicle type dropdown works
16. Vehicle fare config saves correctly
17. Duplicate fare config is blocked
18. Invalid fare values are blocked
19. findZoneByCoordinates works
20. calculateFareForZone works
21. UI works on desktop and mobile

Step 12 — Final Report

When finished, provide a clear final report with:

- Summary of schema analysis
- Important tables discovered
- Files changed
- New files created
- Database changes/migrations
- New routes/endpoints
- UI/UX improvements made
- Testing completed
- Any known limitations
- Commands needed to run the app
- Commands needed to run migrations
- Any recommended next steps

Very important:
Work step by step.
Analyze first, then code.
After coding, review your own code and fix issues before final response.